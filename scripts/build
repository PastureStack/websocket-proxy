#!/bin/bash
set -e

source $(dirname $0)/version

cd $(dirname $0)/..

mkdir -p bin
CGO_ENABLED=0 go build -mod=vendor -trimpath -buildvcs=false -tags "netgo osusergo" -ldflags "-buildid= -X main.VERSION=$VERSION -s -w" -o bin/websocket-proxy
